{
  "name": "stadnup",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://whole-heat.surge.sh",
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "react-scripts": "0.8.5"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-tag-input": "^4.6.2",
    "surge": "^0.18.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && surge -p build --domain whole-heat.surge.sh"
  }
}